#
# include/swtpm/Makefile.am
#
# For the license, see the COPYING file in the root directory.
#

swtpmincludedir = $(includedir)/swtpm

swtpminclude_HEADERS = \
	tpm_ioctl.h

if SHARED_LIBRARY_BUILD

swtpminclude_HEADERS += \
	swtpm.h

endif
